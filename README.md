# golang is compiled language 

# Types

walrus operator must be used inside a function :=

variable which has capital first letter makes variable as global

to search package in go is pkg.go.dev

# memory management in golang

memory allocation and deallocation happens automatically

two methods for memory management

new() and make()

new gives zeroed storage (initially can't put any data same as no INIT point)

make gives non-zeroed storage (initially can put data same as INIT point)

pointers

# slices in golang (used in databases so learn it well !!)

under the hood arrays

... variadic expansion operator

# maps in golang (hash table, key value pair)

# structs in golang

# Defer Statements in golang
 defer matlab hindi me talna immediate next statement ko
 end tak talna

 if multiple defer statements then they are deferred in LIFO order
 defers are pushed in a stack and at last these stack are executed


# handling files in golang (Text files)


# web requests in golang

# to see the relevant package corresponding to golang

# go to website pkg.go.dev

# We send data in api req as json format or URL encoded format

# Creating and handling json 

# Go mod - tooling

# semantic versioning

file:///home/harshit-dixit/Pictures/Screenshots/Screenshot%20from%202025-11-15%2016-11-28.png

